# Benchmark Files

This directory stores ranking benchmark manifests in JSONL format.

`example_oral_polymer_ranking.jsonl` is a toy dataset for exercising the loader and evaluation code. It is not a scientific benchmark.
