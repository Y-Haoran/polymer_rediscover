# Schema Files

This directory contains starter manifests for candidate normalization and benchmark construction.

- `candidate_polymers_example.tsv`: toy candidate list
- `polymer_synonyms_example.tsv`: toy synonym mapping

Replace these with normalized FDA IID and DailyMed-derived tables once the real data are ready.
